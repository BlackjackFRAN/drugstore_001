@model IEnumerable<drugstore_001.LineaCompra>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.subtotal)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precioCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Compra.idCompra)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Producto.descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.subtotal)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precioCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Compra.idCompra)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Producto.descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idLineaCompra }) |
            @Html.ActionLink("Details", "Details", new { id=item.idLineaCompra }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idLineaCompra })
        </td>
    </tr>
}

</table>
